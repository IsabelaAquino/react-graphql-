type Query {
  users(query: String): [User!]!
}

type Mutation {
  createUser(name: String!, email: String!, phone: String!, password: String!, avatar: String!): User!
}

type User {
  id: ID
  name: String!
  email: String!
  phone: String!
  password: String!
  avatar: String!
}
